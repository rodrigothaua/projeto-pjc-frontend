import React from 'react';
import { useParams } from 'react-router-dom';

function DetalheDesaparecido() {
  const { id } = useParams();

  return (
    <div>
      {/* Implementar a lógica para exibir os detalhes do desaparecido */}
    </div>
  );
}

export default DetalheDesaparecido;